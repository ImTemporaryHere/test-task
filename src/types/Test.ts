export interface iTest {
  label: string;
  score: number | null;
  speed: string | null;
  total: number;
  expSpeed: string,
  concept: string,
  date: string,
  absent?: boolean
  abcent?: boolean
}